OPENGL Init
1. 初始化glfw的环境，用来创建窗口以及上下文环境。
2. 初始化glew的环境，用来获取所在平台的api实现， opengl本身是一个框架，定义了
一个标准流程方法，具体实现依赖于各个显卡厂家，glew捕获的各个api的函数地址。
3. 绘制一个长方形， 也就是两个三角形，其中两点公用，我们只需要提供四个边界点即
可， 然后给出绘制的点的index。
4. 申请buffer，绑定buffer，设置属性，编译着色器，绘制


API 查询网址 https://docs.gl
